name: EnergyConsumptionPrediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "energy_preprocessed.csv"}
      n_estimators: {type: int, default: 100}
    command: "python modelling.py --data_path {data_path} --n_estimators {n_estimators}"
